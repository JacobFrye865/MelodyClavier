{% extends 'base.html.twig' %}

{% block title %}MelodyClavier - Interface Utilisateur{% endblock %}

{% block body %}
    <h1>Interface Utilisateur</h1>{% for user in users %}
    <table>
        <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Email</th>
        </tr>
        
        <tr>
            <td>{{ user.firstname }}</td>
            <td>{{ user.lastname }}</td>
            <td>{{ user.email }}</td>
        </tr>
    </table>
    {% endfor %}

{% endblock %}
